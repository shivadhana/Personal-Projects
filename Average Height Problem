# Question posed
student_heights = input("Input a list of student heights ").split()

# For loop to make range and all height as an int
for n in range(0, len(student_heights)):
  student_heights[n] = int(student_heights[n])
  
# Formula for finding the average height
average = round(sum(student_heights)/len(student_heights))
print(f'The average height is: {average}')
print(f'The tallest person is: {max(student_heights)}')